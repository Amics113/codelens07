{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal default CodeLens desktop permissions",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
